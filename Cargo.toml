[package]
name = "camera_simple_detect"
version = "0.1.0"
edition = "2021"

[dependencies]
windows = { version = "0.58", features = [
  "Win32_Foundation",
  "Win32_MediaFoundation",
  "Win32_System_Com",
] }
